---
const { slug, title, description } = Astro.props;
---

<a
  href={`/blog/${slug}`}
  class="border-[#8c7676] border-2 rounded-xl px-4 py-2 flex flex-col items-start justify-between hover:bg-[#e0d8d8] transition-all duration-100 ease-in"
>
  <h2 class="text-2xl font-semibold">{title}</h2>
  <p class="mt-3">{description}</p>
</a>
